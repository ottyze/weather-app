import Home from "./pages/Home";

const App = () => {
  return (
    <div className="bg-gradient-to-r from-blue-400 to-blue-600 min-h-screen flex justify-center items-center">
      <Home />
    </div>
  );
};

export default App;
